const assert = require('assert')
const Coordinate = require('../lib/coordinate')

describe('Coordinate', () => {

  // it("should calculate the distance from itself", () => {
  //   const a = new Coordinate(0, 0)
  //   assert.equal(a.coord.distanceFrom(a), 0)
  // })

  // it("should calculate the distance from another coordinate along X axis", ()=>{
  //   const a = new Coordinate(0, 0,900)
  //   const b = new Coordinate(1000, 0, 1000)

  //   console.log(a);
  //   console.log(b);

  //   assert.equal(a.coord.distanceFrom(b), 1000)
  // })

  // it("should calculate the distance ", ()=>{
  // 	const a = new Coordinate(0, 0, 1000)
  // 	const b = new Coordinate(3, 4, 900)

  // 	assert.equal(a.coord.distanceFrom(b), 5)
  // })

})
